<% form_for(@chat) do |f| %>
  <%= f.error_messages %>

  <div class="field">
    <%= label_tag :user_id, 'Recipient' %> <%= select_tag :user_id, recipient_options_for_select(@user_id) %>
  </div>

  <div class="field">
    <%= f.label :subject %><br />
    <%= f.text_field :subject %>
  </div>

  <% fields_for(:message) do |message_form| %>
    <%= render 'messages/form', :form => message_form %>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
