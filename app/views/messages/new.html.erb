<h1>New message</h1>

<% form_for(@message) do |f| %>
  <%= f.error_messages %>

  <%= render f %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Back', chat_path(@chat) %>
